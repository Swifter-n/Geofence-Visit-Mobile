import 'package:flutter/material.dart';

class AppColors {
    static const Color primary = Color(0x102A43);
    static const Color secondary = Color(0x486581); 

    static const Color tertiary = Color(0x059669);
    static const Color neutral = Color(0xF0F4F8);
    static const Color card = Colors.white;
    static const Color stroke = Color(0xFFE0E0E0);

  // Text Colors
    static const Color textPrimary = Color(0xFF2D2D2D);
    static const Color textSecondary = Color(0xFF757575);
    static const Color textLight = Colors.white;

  // Additional status from image
    static const Color red = Color(0xFFC62828); 

}